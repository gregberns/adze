
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# kerf internal state (project-identifier is committed separately)
.kerf/
!.kerf/project-identifier
.claude/worktrees/

# Personal machine configs (kept in private gists, not the repo)
examples/greg-berns.yaml
